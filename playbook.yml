---
- name: Pull and Run docker image
  hosts: all
  tasks:
    
    - name: Pull shiv228/calcuator
      docker_image:
        name: shiv228/calcuator
        source: pull
  vars:
    ansible_python_interpreter: /bin/python3
    

